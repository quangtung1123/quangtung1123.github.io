header:
  - titles:
      # @start locale config
      en      : &EN       Archive
      # @end locale config
    url: /archive.html
    
  - titles:
      # @start locale config
      en      : &EN       Documents
      # @end locale config
    url: /documents

  - titles:
      # @start locale config
      en      : &EN       About
      # @end locale config
    url: /about.html

  - titles:
      # @start locale config
      en      : &EN       Donate
      # @end locale config
    url: /donate

  - titles:
      # @start locale config
      en      : &EN       Advanced Search
      # @end locale config
    url: /search